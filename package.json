{
    "name": "monorepo-experiment",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "lerna": "^3.22.0"
    },
    "scripts": {
        "lint": "lerna exec --no-bail yarn lint",
        "lint:ci": "./scripts/ci/lint"
    }
}
